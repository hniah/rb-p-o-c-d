= form_tag(express_checkout_path, method: :patch) do
  = hidden_field_tag 'package_id', package.id
  = submit_tag(number_to_currency(package.price, precision: 0) , class: "btn", data: { test: package.id })
