- @promotion_blocks.each do |block|
  .block-item
    .block_image
      %img{ :src => block.image.url(:thumb)}
    - if block.description
      .block-descrition
        = block.description.html_safe
    - if block.url.present?
      .block-url
        = link_to "Click here to view" , block.url
